{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Team</h1>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
